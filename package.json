{
  "name": "poser",
  "scripts": {
    "heroku-prebuild": "npx pnpm i -r --store=node_modules/.pnpm-store",
    "start": "pnpm -r start",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean"
  },
  "cacheDirectories": [
    "node_modules/.pnpm"
  ],
  "dependencies": {
    "pnpm": "^6.2.1"
  }
}
