{
  "name": "poser",
  "scripts": {
    "start": "build && pnpm -r --parallel start",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --parallel build"
  },
  "dependencies": {
    "esbuild": "^0.11.14",
    "pnpm": "^6.2.1",
    "rollup": "^2.45.2"
  }
}
