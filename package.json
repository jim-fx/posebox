{
  "name": "poser",
  "scripts": {
    "start": "pnpm -r start",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm i -r && pnpm -r build",
    "clean": "pnpm -r clean"
  },
  "cacheDirectories": [
    "/app/.pnpm-store",
    "node_modules/.pnpm"
  ],
  "dependencies": {
    "pnpm": "^6.2.1"
  }
}
