{
  "name": "@poser/api",
  "version": "1.0.0",
  "description": "",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --outdir=dist --bundle --format=esm && tsc --declaration --emitDeclarationOnly",
    "dev": "esbuild src/index.ts --outdir=dist --bundle --watch --format=esm",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@poser/types": "link:../types",
    "esbuild": "^0.11.13",
    "socket.io-client": "^4.0.1",
    "svelte": "^3.37.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
